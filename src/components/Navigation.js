import { Navbar, Container } from 'react-bootstrap'

function Navigation() {
    return (
        <Navbar bg="light" expand="lg">
            <Container fluid>

            </Container>
        </Navbar>
    )
}

export default Navigation;