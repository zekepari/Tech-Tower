import { Navbar, Container } from 'react-bootstrap'

function Navbar() {
    return (
        <Navbar bg="light" expand="lg">
            <Container fluid>

            </Container>
        </Navbar>
    )
}

export default Navbar;